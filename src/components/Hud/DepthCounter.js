import React from 'react';

const DepthCounter = ({ depth }) => {
    return (
        <div id="depthCounter">
            Profundidad: {depth} m
        </div>
    );
};

export default DepthCounter;
